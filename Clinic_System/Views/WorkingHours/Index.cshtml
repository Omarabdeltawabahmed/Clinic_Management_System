@model IEnumerable<Clinic_System.Models.WorkingHour>

@{
    ViewData["Title"] = "Working Hours Management";
}

<div class="container py-4">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <div>
            <h2 class="fw-bold text-primary mb-1">Working Hours Management</h2>
            <p class="text-muted">Set and manage weekly working schedules for clinic doctors.</p>
        </div>

        <a asp-action="Create" class="btn btn-primary rounded-pill px-4 shadow-sm">
            <i class="bi bi-clock-history me-1"></i> Add Working Schedule
        </a>

    </div>

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden">

        <div class="table-responsive">

            <table class="table table-hover align-middle mb-0">

                <thead class="table-light">

                    <tr>
                        <th scope="col" class="ps-4">Doctor</th>
                        <th scope="col" class="text-center">Day</th>
                        <th scope="col" class="text-center">Start Time</th>
                        <th scope="col" class="text-center">End Time</th>
                        <th scope="col" class="text-end pe-4">Actions</th>
                    </tr>

                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="5" class="text-center py-5 text-muted">
                                <i class="bi bi-calendar-x display-6 d-block mb-2 text-secondary"></i>
                                No working hours scheduled yet.
                            </td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model)
                        {
                            <tr>

                                <td class="ps-4">
                                    <div class="fw-bold text-dark">@(item.Doctor?.Name ?? "Unassigned")</div>
                                </td>

                                <td class="text-center">
                                    <span class="badge bg-info-subtle text-info-emphasis fw-semibold px-3 py-2 rounded-pill">
                                        @item.Day
                                    </span>
                                </td>

                                <td class="text-center">
                                    <span class="fw-semibold text-success"><i class="bi bi-clock me-1"></i>@DateTime.Today.Add(item.StartTime).ToString("hh:mm tt")</span>
                                </td>

                                <td class="text-center">
                                    <span class="fw-semibold text-danger"><i class="bi bi-clock-fill me-1"></i>@DateTime.Today.Add(item.EndTime).ToString("hh:mm tt")</span>
                                </td>

                                <td class="text-end pe-4">
                                    <div class="btn-group gap-1">

                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning rounded-circle" title="Edit">
                                            <i class="bi bi-pencil"></i>
                                        </a>

                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger rounded-circle" title="Delete">
                                            <i class="bi bi-trash"></i>
                                        </a>

                                    </div>
                                </td>

                            </tr>
                        }
                    }

                </tbody>

            </table>

        </div>

    </div>

</div>